  <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path @list.photo.key, height: 300 %>')">
    <div class="container">
      <h1>Add new bookmark to <%= @list.name.downcase %> list</h1>
      <hr>
      <%= link_to "Back to main page", lists_path, class: "btn btn-flat" %>
    </div>
  </div>

<div class="container mt-3">
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.input :comment, placeholder: 'Comment' %>
    <%= f.association :movie, prompt: 'Select a movie' %>
    <%= f.submit class: 'btn btn-flat' %>
  <% end %>
</div>
